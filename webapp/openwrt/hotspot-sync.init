#!/bin/sh /etc/rc.common
# OpenWrt Init Script for Voucher Sync Service
# Save as: /etc/init.d/hotspot-sync

START=99
STOP=10

USE_PROCD=1

start_service() {
    procd_open_instance
    procd_set_param command /usr/bin/hotspot-sync-daemon
    procd_set_param respawn 3600 5 0
    procd_set_param stdout 1
    procd_set_param stderr 1
    procd_close_instance
}

stop_service() {
    killall hotspot-sync-daemon 2>/dev/null
}