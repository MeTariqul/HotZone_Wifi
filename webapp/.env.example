# Neon Database URL - replace with your actual connection string
DATABASE_URL="postgresql://user:password@your-neon-host/neondb?sslmode=require"

# Admin dashboard password (used for session login at /admin)
ADMIN_PASSWORD="change-me-strong-password"

# Shared key for router voucher sync (/api/vouchers/sync)
SYNC_API_KEY="change-me-sync-key"

# Router control — optional on Vercel (admin uses DB snapshot + command queue).
# Set ROUTER_URL only if this app runs on the same LAN as the router and needs
# direct outbound calls (authorize fallback). Splash still uses ROUTER_SECRET
# as the Bearer value the captive-portal browser sends to the router.
ROUTER_URL=""
ROUTER_SECRET="your-router-secret"
